<UserControl x:Class="VisionWorkOrderApp.Views.InspectionResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisionWorkOrderApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 필터 영역 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="결과필터" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding FilterOptions}"
                      SelectedItem="{Binding SelectedFilter}"
                      Width="100"
                      Height="30"/>
            <Button Content="삭제"
                    Command="{Binding DeleteCommand}"
                    Width="60"
                    Height="30"
                    Margin="10,0,0,0"/>
        </StackPanel>

        <!-- 결과 목록 -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding FilteredResults}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectedItem="{Binding SelectedResult}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="시간" Binding="{Binding TimeStamp}" Width="150"/>
                <DataGridTextColumn Header="제품명" Binding="{Binding ProductName}" Width="120"/>
                <DataGridTextColumn Header="결과" Binding="{Binding Label}" Width="80"/>
                <DataGridTextColumn Header="신뢰도" Binding="{Binding Confidence}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
